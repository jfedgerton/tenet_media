# The labeled sentence corpus is large; store it with Git LFS so the repo stays
# clonable and GitHub's 100 MB per-file limit is not hit. Requires `git lfs install`.
*.parquet filter=lfs diff=lfs merge=lfs -text
